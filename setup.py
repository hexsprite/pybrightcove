from setuptools import setup, find_packages
import os

version = '1.0'

setup(name='pybrightcove',
      version=version,
      description="",
      long_description='',
      # Get more strings from http://www.python.org/pypi?%3Aaction=list_classifiers
      classifiers=[
        ],
      keywords='',
      author='',
      author_email='',
      url='',
      license='',
      packages=find_packages(exclude=['ez_setup']),
#      namespace_packages=
      include_package_data=True,
      zip_safe=False,
      install_requires=[
          # -*- Extra requirements: -*-
          ],
      )
